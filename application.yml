server:
  port: 8090

management:
  security:
    enabled: false    

promotion: action

spring:
  application:
    name: awesome-media-localhost
  datasource:
    platform: h2
    initialize: true
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:mediadb;
    username: sa
    password: null
  h2:
    console:
      enabled: true
      